# 🧮 محاسبه‌گر حقوق و دستمزد پیشرفته

یک ابزار کامل و مدرن برای محاسبه حقوق و دستمزد بر اساس قوانین وزارت کار ایران - سال ۱۴۰۴

## ✨ ویژگی‌ها

### 🏗️ معماری ماژولار

- **ساختار لایه‌ای**: جداسازی کامل قوانین، ثابت‌ها و منطق محاسبه
- **قابل تست**: تست‌های واحد کامل برای تمام توابع
- **قابل توسعه**: اضافه کردن قوانین جدید بدون تغییر هسته
- **نگهداری آسان**: خوانایی بالا و مستندات کامل

### 📊 قابلیت‌های محاسباتی

- ✅ محاسبه حقوق ناخالص به خالص
- ✅ محاسبه حقوق خالص به ناخالص (معکوس)
- ✅ محاسبه بر اساس حداقل دستمزد
- ✅ پشتیبانی از اضافه‌کاری (عادی، شب، تعطیلات)
- ✅ محاسبه مزایا (حق مسکن، بن، حق اولاد، مأموریت)
- ✅ مالیات تصاعدی با معافیت‌های قانونی
- ✅ بیمه تامین اجتماعی
- ✅ پاداش سابقه کار
- ✅ تخفیف مناطق کمتر توسعه‌یافته

### 🎯 رابط کاربری

- فرم‌های هوشمند با اعتبارسنجی
- نمایش جزئیات کامل محاسبه
- ذخیره خودکار اطلاعات در جلسه
- طراحی واکنش‌گرا برای موبایل و دسکتاپ

## 📁 ساختار پروژه

```
salary/
├── index.ts                    # موتور اصلی محاسبه
├── constants.ts                # ثابت‌های قانونی سال ۱۴۰۴
├── salary.types.ts             # انواع داده‌ها
├── SalaryPage.tsx              # رابط کاربری React
├── salary.test.ts              # تست‌های واحد
├── README.md                   # مستندات
├── utils/
│   └── tax.ts                 # ابزارهای مالیات
└── rules/
    ├── baseSalary.ts          # قوانین حقوق پایه
    ├── overtime.ts            # قوانین اضافه‌کاری
    ├── allowances.ts          # قوانین مزایا
    ├── insurance.ts           # قوانین بیمه
    └── deductions.ts          # قوانین کسورات
```

## 🚀 شروع به کار

### نصب و راه‌اندازی

```bash
# نصب وابستگی‌ها
npm install

# اجرای تست‌ها
npm test salary

# اجرای پروژه در حالت توسعه
npm run dev
```

### استفاده از موتور محاسبه

```typescript
import { calculateSalary, calculateMinimumWage } from './features/salary';

// محاسبه حقوق کامل
const result = calculateSalary({
  baseSalary: 15000000,
  workingDays: 30,
  workExperienceYears: 5,
  overtimeHours: 10,
  isMarried: true,
  numberOfChildren: 2,
  hasWorkerCoupon: true,
  hasTransportation: true,
  isDevelopmentZone: false
});

console.log(result);
// {
//   grossSalary: 21450000,
//   netSalary: 18520000,
//   details: { ... },
//   summary: { ... }
// }

// محاسبه حداقل دستمزد
const minWage = calculateMinimumWage({
  workExperienceYears: 3,
  isMarried: true,
  numberOfChildren: 1
});
```

## 📋 قوانین پیاده‌سازی شده

### 💰 مالیات بر حقوق

- **معافیت سالانه**: ۲۸۸ میلیون تومان
- **نرخ‌های تصاعدی**: ۰٪، ۱۰٪، ۱۵٪، ۲۰٪، ۲۵٪، ۳۰٪
- **تخفیف مناطق کمتر توسعه‌یافته**: ۵۰٪

### 🏥 بیمه تامین اجتماعی

- **سهم کارگر**: ۷٪
- **سهم کارفرما**: ۲۳٪
- **مجموع**: ۳۰٪

### 💵 حداقل دستمزد ۱۴۰۴

- **حقوق پایه**: ۸,۱۰۰,۰۰۰ تومان
- **کمک هزینه مسکن**: ۲,۵۰۰,۰۰۰ تومان
- **کمک هزینه غذا**: ۱,۰۰۰,۰۰۰ تومان
- **حق اولاد**: ۱۵۰,۰۰۰ تومان به ازای هر فرزند
- **پاداش سابقه**: ۵۰۰,۰۰۰ تومان به ازای هر ۵ سال

### ⏰ اضافه‌کاری

- **عادی**: ۱.۴ برابر نرخ عادی
- **شب**: ۱.۵ برابر نرخ عادی
- **تعطیلات رسمی**: ۱.۸ برابر نرخ عادی

## 🧪 تست‌ها

پروژه شامل ۳۳ تست واحد برای پوشش کامل تمام قوانین:

```bash
# اجرای تمام تست‌های حقوق
npm test salary

# اجرای تست‌ها با مشاهده پوشش
npm run test:salary -- --coverage

# اجرای تست‌ها در حالت watch
npm run test:salary -- --watch
```

### دسته‌بندی تست‌ها

- **Salary Engine Tests**: تست‌های اصلی موتور محاسبه
- **Base Salary Rules Tests**: تست‌های حقوق پایه
- **Overtime Rules Tests**: تست‌های اضافه‌کاری
- **Allowances Rules Tests**: تست‌های مزایا
- **Insurance Rules Tests**: تست‌های بیمه
- **Tax Rules Tests**: تست‌های مالیات
- **Integration Tests**: تست‌های یکپارچگی
- **Performance Tests**: تست‌های عملکرد

## 📖 مستندات API

### تابع اصلی `calculateSalary`

```typescript
function calculateSalary(input: SalaryInput): SalaryOutput
```

#### ورودی‌ها

```typescript
interface SalaryInput {
  baseSalary: number;                    // حقوق پایه ماهانه
  workingDays?: number;                  // روزهای کاری (پیش‌فرض: ۳۰)
  workExperienceYears?: number;          // سابقه کار (سال)
  overtimeHours?: number;                // اضافه‌کاری عادی (ساعت)
  nightOvertimeHours?: number;           // اضافه‌کاری شب (ساعت)
  holidayOvertimeHours?: number;         // اضافه‌کاری تعطیلات (ساعت)
  missionDays?: number;                  // روزهای مأموریت
  isMarried?: boolean;                   // وضعیت تاهل
  numberOfChildren?: number;            // تعداد فرزندان
  hasWorkerCoupon?: boolean;             // بن کارگری
  hasTransportation?: boolean;           // کمک هزینه ایاب‌وذهاب
  otherBenefits?: number;                // سایر مزایا
  otherDeductions?: number;              // سایر کسورات
  isDevelopmentZone?: boolean;           // منطقه کمتر توسعه‌یافته
  customInsuranceRate?: number;          // نرخ سفارشی بیمه
}
```

#### خروجی‌ها

```typescript
interface SalaryOutput {
  grossSalary: number;                   // حقوق ناخالص
  netSalary: number;                     // حقوق خالص
  baseSalary: number;                    // حقوق پایه محاسبه شده
  details: SalaryDetails;                // جزئیات کامل
  summary: SalarySummary;                // خلاصه اطلاعات
}
```

## 🔧 تنظیمات و سفارشی‌سازی

### به‌روزرسانی قوانین سالانه

برای به‌روزرسانی قوانین جدید، فقط فایل `constants.ts` را ویرایش کنید:

```typescript
// مثال: به‌روزرسانی حداقل دستمزد سال جدید
export const MINIMUM_WAGE_1405 = 9_500_000;
export const ANNUAL_TAX_EXEMPTION_1405 = 350_000_000;
```

### اضافه کردن قانون جدید

۱. تابع قانون را در پوشه `rules/` اضافه کنید
۲. تست واحد بنویسید
۳. در موتور اصلی `index.ts` فراخوانی کنید

## 🎨 استفاده در رابط کاربری

```tsx
import { calculateSalary } from './features/salary';

function SalaryCalculator() {
  const [result, setResult] = useState(null);
  
  const handleCalculate = (formData) => {
    const salaryResult = calculateSalary(formData);
    setResult(salaryResult);
  };
  
  return (
    <div>
      {/* فرم ورودی */}
      <SalaryForm onSubmit={handleCalculate} />
      
      {/* نمایش نتایج */}
      {result && <SalaryResult result={result} />}
    </div>
  );
}
```

## 🐛 عیب‌یابی

### خطاهای رایج

1. **"حقوق پایه باید عددی مثبت باشد"**
   - بررسی کنید که ورودی عددی و مثبت باشد

2. **"حقوق خالص باید بزرگتر از صفر باشد"**
   - در محاسبه معکوس، حقوق خالص باید مثبت باشد

3. **نتیجه مالیات صفر است**
   - بررسی کنید که درآمد از معافیت سالانه بیشتر باشد

### دیباگ کردن

```typescript
// فعال کردن حالت دیباگ
const result = calculateSalary(input);
console.log('Tax Details:', result.details.tax);
console.log('Insurance Details:', result.details.insurance);
```

## 🤝 مشارکت

برای مشارکت در پروژه:

۱. یک شاخه جدید ایجاد کنید

```bash
git checkout -b feature/new-feature
```

۲. تغییرات را اعمال کنید
۳. تست‌ها را اجرا کنید

```bash
npm test salary
```

۴. یک Pull Request ایجاد کنید

## 📄 مجوز

این پروژه تحت مجوز MIT منتشر شده است.

## 🙏 تشکر

- قوانین وزارت کار و امور اجتماعی ایران
- سازمان امور مالیاتی کشور
- سازمان تامین اجتماعی

---

**نسخه**: 2.0.0  
**آخرین به‌روزرسانی**: بهمن ۱۴۰۳  
**سازگار با**: React 18+, TypeScript 5+, Node.js 18+
