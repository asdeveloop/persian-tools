# ابزارهای جامع PDF

مجموعه کامل ابزارهای کاربردی برای مدیریت و پردازش فایل‌های PDF در پروژه persian-tools.

## 📦 ساختار ماژول

```
pdf-tools/
├── convert/
│   ├── image-to-pdf.tsx          # تبدیل عکس به PDF
│   ├── image-to-pdf.logic.ts     # منطق تبدیل عکس به PDF
│   ├── pdf-to-image.tsx          # تبدیل PDF به عکس
│   ├── pdf-to-image.logic.ts     # منطق تبدیل PDF به عکس
│   └── *.test.ts                 # تست‌ها
├── compress/
│   ├── compress-pdf.tsx          # فشرده‌سازی PDF
│   ├── compress-pdf.logic.ts     # منطق فشرده‌سازی
│   └── *.test.ts                 # تست‌ها
├── merge/
│   ├── merge-pdf.tsx             # ادغام PDF
│   ├── merge-pdf.logic.ts        # منطق ادغام
│   └── *.test.ts                 # تست‌ها
├── split/
│   ├── split-pdf.tsx             # تقسیم PDF
│   ├── split-pdf.logic.ts        # منطق تقسیم
│   └── *.test.ts                 # تست‌ها
├── security/
│   ├── encrypt-pdf.tsx           # رمزگذاری PDF
│   ├── encrypt-pdf.logic.ts      # منطق رمزگذاری
│   ├── decrypt-pdf.tsx           # حذف رمز PDF
│   ├── decrypt-pdf.logic.ts      # منطق حذف رمز
│   └── *.test.ts                 # تست‌ها
├── watermark/
│   ├── add-watermark.tsx         # افزودن واترمارک
│   ├── add-watermark.logic.ts    # منطق واترمارک
│   └── *.test.ts                 # تست‌ها
├── paginate/
│   └── add-page-numbers.ts       # شماره صفحه (آینده)
├── extract/
│   └── extract-pages.ts          # استخراج صفحات (آینده)
├── types.ts                      # تایپ‌های مشترک
├── index.ts                      # خروجی اصلی ماژول
├── PdfToolsPage.tsx              # صفحه اصلی ابزارها
└── README.md                     # این مستندات
```

## 🛠️ ابزارهای موجود

### تبدیل (Convert)
- **تبدیل عکس به PDF**: تبدیل چندین تصویر JPG/PNG به یک فایل PDF با تنظیمات پیشرفته
- **تبدیل PDF به عکس**: تبدیل صفحات PDF به تصاویر PNG/JPG با کیفیت قابل تنظیم

### فشرده‌سازی (Compress)
- **فشرده‌سازی PDF**: کاهش حجم فایل PDF با حفظ کیفیت

### ادغام (Merge)
- **ادغام PDF**: ترکیب چندین فایل PDF در یک فایل واحد با قابلیت مرتب‌سازی

### تقسیم (Split)
- **تقسیم PDF**: جدا کردن صفحات PDF بر اساس محدوده یا به صورت تک‌صفحه‌ای

### امنیت (Security)
- **رمزگذاری PDF**: محافظت از فایل PDF با رمز عبور و مجوزهای دسترسی
- **حذف رمز PDF**: باز کردن قفل فایل‌های PDF رمزگذاری شده

### واترمارک (Watermark)
- **افزودن واترمارک**: اضافه کردن متن یا لوگو به صفحات PDF با تنظیمات کامل

## 🚀 ویژگی‌ها

### قابلیت‌های عمومی
- **رابط کاربری مدرن**: طراحی واکنشگرا با Tailwind CSS
- **پردازش محلی**: فایل‌ها به صورت محلی پردازش می‌شوند و امن باقی می‌مانند
- **نشانگر پیشرفت**: نمایش وضعیت پردازش به صورت زنده
- **پشتیبانی از drag & drop**: آپلود آسان فایل‌ها با کشیدن و رها کردن

### قابلیت‌های فنی
- **TypeScript**: کدنویسی با تایپ‌های قوی و امن
- **تست کامل**: پوشش تستی برای تمام ابزارها
- **مدولار**: ساختار ماژولار برای توسعه آسان
- **بهینه‌سازی**: پردازش بهینه برای فایل‌های حجیم

## 📖 نحوه استفاده

### استفاده از کامپوننت‌ها

```typescript
import { PdfToolsPage } from './features/pdf-tools';

// در مسیر اصلی
function App() {
  return (
    <Router>
      <Routes>
        <Route path="/pdf-tools" element={<PdfToolsPage />} />
        <Route path="/pdf-tools/convert/image-to-pdf" element={<ImageToPdfPage />} />
        {/* مسیرهای دیگر */}
      </Routes>
    </Router>
  );
}
```

### استفاده از توابع منطقی

```typescript
import { 
  imagesToPdfBytes, 
  compressPdf, 
  mergePdfs,
  splitPdf,
  encryptPdf,
  addWatermark 
} from './features/pdf-tools';

// تبدیل عکس به PDF
const pdfBytes = await imagesToPdfBytes(imageItems, {
  orientation: 'portrait',
  margin: 'small',
  pageSize: 'a4',
  quality: 0.8
});

// فشرده‌سازی PDF
const compressed = await compressPdf(pdfBytes, { quality: 0.8 });

// ادغام PDFها
const merged = await mergePdfs(pdfItems, { preserveBookmarks: true });
```

## 🧪 تست‌ها

برای اجرای تست‌ها:

```bash
npm test -- src/features/pdf-tools
```

تست‌ها شامل:
- تست‌های واحد برای تمام توابع منطقی
- تست‌های اعتبارسنجی ورودی‌ها
- تست‌های خطا و شرایط مرزی
- تست‌های یکپارچگی

## 🔧 تنظیمات

### وابستگی‌ها
- `pdf-lib`: کتابخانه اصلی کار با PDF
- `react`: فریمورک رابط کاربری
- `typescript`: تایپ‌اسکریپت
- `vitest`: فریمورک تست

### پیکربندی
- تمام ابزارها از تنظیمات پیش‌فرض استفاده می‌کنند
- امکان سفارشی‌سازی کامل تنظیمات برای هر ابزار
- پشتیبانی از زبان فارسی و راست‌چین

## 🎯 نقشه راه

### ویژگی‌های آینده
- **تبدیل Word به PDF**: تبدیل فایل‌های Word به PDF
- **استخراج متن**: استخراج متن از فایل‌های PDF
- **شماره صفحه**: افزودن شماره صفحه به فایل‌های PDF
- **استخراج صفحات**: استخراج صفحات خاص از PDF
- **امضای دیجیتال**: افزودن امضای دیجیتال به PDF

### بهبودهای فنی
- **پردازش سرور**: قابلیت پردازش در سمت سرور برای فایل‌های بسیار حجیم
- **پشتیبانی از فرمت‌های بیشتر**: پشتیبانی از فرمت‌های تصویری دیگر
- **بهینه‌سازی عملکرد**: بهبود سرعت پردازش برای فایل‌های بزرگ
- **حافظه نهان**: ذخیره تنظیمات کاربر

## 🤝 مشارکت

برای مشارکت در توسعه این ماژول:
1. ایجاد شاخه جدید از main
2. توسعه ویژگی مورد نظر
3. افزودن تست‌های لازم
4. ارسال Pull Request

## 📄 مجوز

این ماژول تحت مجوز پروژه اصلی قرار دارد.

## 🙏 تشکر

تشکر از:
- تیم توسعه pdf-lib برای کتابخانه عالی
- جامعه React و TypeScript
- کاربران ایرانی برای بازخورد و پیشنهادات
