# Locale & Output

- UI text must be Persian (fa-IR) and RTL when the project is Persian-facing.
- Use Persian digits for display; keep internal values standard (e.g., number/string).
- Dates: specify calendar (Jalali/Gregorian). Prefer Jalali when user-facing in Iran context.
